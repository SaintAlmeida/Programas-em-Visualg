algoritmo "500 a 0"
var
c,soma: inteiro
inicio
c <- 500
soma <- 0
repita
      escreval(C)
      escreval("+")
      c <- c - 50
      soma <- soma + c
ate(c<0)
       escreval("a soma da expressão é :", soma)
fimalgoritmo
